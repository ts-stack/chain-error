{
	"name": "@restify-ts/chain-error",
	"version": "1.0.0",
	"description": "Richer JavaScript errors",
  "main": "dist/index",
  "typings": "dist/index",
  "repository": "https://github.com/restify-ts/chain-error.git",
  "homepage": "https://github.com/restify-ts/chain-error",
	"license": "MIT",
	"scripts": {
		"test": "npm run rm-dists && tsc --project test && jasmine",
		"rm-dists": "rimraf dist/* && rimraf dist-test/*",
		"build": "rimraf dist/* && tsc",
		"tslint": "tslint --project ./tsconfig.json '{src}/**/*.ts' --fix"
	},
	"dependencies": {
		"assert-plus": "^1.0.0"
	},
	"devDependencies": {
		"@types/assert-plus": "^1.0.4",
		"@types/jasmine": "^2.8.11",
		"jasmine": "^3.3.0",
		"prettier": "^1.14.3",
		"rimraf": "^2.6.2",
		"tslint": "^5.11.0",
		"tslint-config-prettier": "^1.15.0",
		"tslint-plugin-prettier": "^2.0.1",
		"typescript": "^3.1.6"
	}
}
